<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- TheaterMapper.xml -->


<mapper namespace="repository.mapper.TheaterMapper">
	<cache />

 	<resultMap id="theaterBaseResultMap" type="theater">
		<id column="theater_num" jdbcType="BIGINT" property="theaterNum" />
		<id column="movie_num" jdbcType="BIGINT" property="movieNum" />
		<result column="theater_name" jdbcType="VARCHAR" property="theaterName" />
		<result column="theater_area" jdbcType="VARCHAR" property="theaterArea" />
	</resultMap>
	
	<!-- 01_02. 게시글 레코드 갯수 -->
	<select id="countArticle" resultType="int" >
		SELECT COUNT(*) FROM theater
		<!-- WHERE절을 include 태그로 삽입 -->
 		<include refid="search"></include> 
	</select>

	<!-- 01. 게시글 전체 목록 조회 및 검색조회까지 -->
	<select id="listAll" resultType="model.Theater" parameterType="Long"
		resultMap="theaterBaseResultMap">
		SELECT theater_num, theater_name, theater_area
		FROM theater
		<!-- WHERE절을 include 태그로 삽입 -->
		<include refid="search"></include>
		ORDER BY theater_num desc
	</select>

	<!-- 02. 게시글 작성 -->
	<insert id="insertTheater" parameterType="Theater">
		INSERT INTO theater (
		theater_num,
		theater_name,
		theater_area,

		) VALUES (
		THEATER_NUM_SEQ.NEXTVAL,
		#{theaterName},
		#{theaterArea}
		)
	</insert>

	<!-- 05. 게시글 수정처리 -->
	<update id="updateTheater" parameterType="Theater">
		UPDATE theater SET
		theater_name = #{theaterName},
		theater_area = #{theaterArea}
		WHERE theater_num = #{theaterNum}
	</update>

	<!-- 06. 게시글 삭제처리 -->
	<delete id="deleteTheater" parameterType="Long">
		DELETE FROM theater
		WHERE theater_num = #{bno}
	</delete>

	<!-- sql code 조각 -->
	<!-- 반복되는 sql의 일부를 sql태그를 이용하여 따로 빼둘수 있다. -->
	<sql id="search">
		<choose>
			<!-- 검색옵션이 전체 검색일 경우 -->
			<when test="searchOption == 'all'">
				WHERE theater_name like '%'||#{keyword}||'%'
				OR
				theater_area like '%'||#{keyword}||'%'
			</when>
			<!-- 전체 검색이 아닐 경우 -->
			<otherwise>
				WHERE ${searchOption} like '%'||#{keyword}||'%'
			</otherwise>
		</choose>
	</sql>

</mapper>